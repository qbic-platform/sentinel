# specify path to qbic.conf or leave blank
# default is the same as QbicCore
# qbic_conf=/root/.qbiccore/qbic.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
